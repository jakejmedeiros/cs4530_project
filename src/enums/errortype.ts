export enum ErrorType {
    DIVIDE_BY_ZERO = "Attempting to divide by zero",
    INVALID_DATA_TYPE = "Attempting calculations with an invalid data type",
    BAD_REFERENCE = "Attempting an invalid reference"
}